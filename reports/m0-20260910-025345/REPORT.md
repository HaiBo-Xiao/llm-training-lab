# M0 最小训练检查

Run ID：`m0-20260910-025345`。日期：2026-09-10。
阶段：M0 / A0，已通过。原运行由学习者执行，环境重建与主机资源核验由助手执行。

研究问题：目标 GPU 能否完成参数更新，并从保存的训练状态恢复出一致的下一步？保存的配置能否重建可运行的最小环境？

## 配置与预算

- 单个参数 w，初值 0，目标值 3；损失为 `(w - 3)^2`。
- SGD，学习率 0.1，动量 0.9。
- 无随机初始化、无随机采样，因此本次随机种子不适用。
- 固定执行两步连续更新，以及一次恢复后的更新；断言失败即停止。
- 代码以本目录 `check_training.py` 快照为准。原运行时采用快照记录；后续核验时该代码已包含在 commit `4996a1f4a00178c8ee331f13166d29e9839a21f8` 中，不将这个后创建的 commit 冒充原运行时版本。
- 脚本 SHA-256：`bb2cc40b20b6325e5f669ebee13cdcbee74286079c7a2693eb3b735d62d8ae8e`。
- 锁文件 SHA-256：`1467968414695d8c37949b206bed2e847ed6b58afb3e42aa5184da25e8685dc4`。
- 本次不涉及数据集、tokenizer、评测集或训练 token；吞吐和峰值显存未测量。

## 原运行结果

- 第一步：更新前 loss=9，更新后 w=0.6。
- 第二步：连续训练与恢复训练均得到 w=1.62。
- 数值检查通过，进程退出码为 0。

## 环境重建结果

执行入口（从项目根目录）：

```bash
bash reports/m0-20260910-025345/rebuild.sh
```

前提是目标 WSL 主机已有 uv、Python 3.12.3、可用 NVIDIA 驱动，以及可用的软件包缓存或下载源。脚本只复制代码与环境定义，在新建临时目录执行 `uv sync --locked`，不复制原 `.venv`。

- 实际执行时间：2026-09-10 03:21:57 +08:00 开始，见日志。
- 新环境路径：`/tmp/llm-m0-rebuild-nKeYlZ/.venv`；实际解释器路径断言通过。
- Python 3.12.3、PyTorch 2.11.0+cu128、NumPy 2.5.3；GPU 为 RTX 4070 SUPER。
- GPU 计算、梯度、参数更新、内存快照恢复均通过。
- `diff` 确认运行输出与原实验一致，`cmp` 确认锁文件未变化。
- 整体重建检查退出码为 0。
- 首次沙箱运行因 uv 缓存目录只读而失败；获得沙箱外执行授权后重试通过。这是执行权限限制，不是依赖或训练失败。

此结论限定为同一 WSL 主机、已有解释器和驱动下的新虚拟环境重建；复用了 uv 软件包缓存，未验证从零安装操作系统、驱动或离线分发。

## 资源补充与存储

Windows 主机可见内存约 31.8 GiB，WSL 可见约 15 GiB。Ubuntu 虚拟磁盘位于 C 盘，C 盘实际空闲约 294 GiB；不能将 WSL 文件系统报告的 934 GiB 当作实际磁盘预算。原始查询结果摘录见 `host_resources.json`。

数据、权重、检查点和报告的位置及保留方式见 [环境与存储约定](../../ENVIRONMENT.md)。每周投入和 GPU 可用时间未登记，不为此编造固定训练计划。

## 证据

- `check_training.py`：原执行代码与配置快照。
- `pyproject.toml`、`uv.lock`、`.python-version`：环境定义。
- `environment.txt`、`host_resources.json`：环境与主机资源信息。
- `command.txt`、`output.txt`、`exit_code.txt`：原运行命令与结果。
- `run.json`：本次实验配置和证据索引。
- `rebuild.sh`：完整重建与检查入口。
- `rebuild_log.txt`：重建命令、安装版本、实际解释器与 GPU 信息。
- `rebuild_output.txt`、`rebuild_exit_code.txt`：重建输出与退出码。
- `rebuild_path.txt`：本次临时目录路径；临时目录不作为长期证据。
- `rebuild_sandbox_failure.txt`：首次沙箱权限失败记录。

## A0 验收

- [x] 确认目标 GPU，记录主机内存、实际空闲磁盘、系统和软件版本。
- [x] CUDA 计算、反向传播、非零梯度与参数更新检查通过，数值有限。
- [x] 最小参数与优化器状态保存加载后可继续更新。
- [x] 根据保存的代码与锁文件，在独立虚拟环境重建并重复检查。
- [x] 本次运行的代码、配置、命令、日志可以对应。

保存恢复使用内存中的 BytesIO，未验证磁盘检查点或独立进程恢复；不据此声称完整模型训练可恢复。这些更完整的恢复验证在 M1 实际训练时完成。

下一步：进入 M1，从字符编码、输入/目标位移与 next-token prediction 开始。
