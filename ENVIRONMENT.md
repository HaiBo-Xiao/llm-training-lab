# 环境、重建与存储约定

检查日期：2026-09-10。适用阶段：M0 与后续 M1 的最小起点。
验收证据见 [M0 报告](reports/m0-20260910-025345/REPORT.md)。

## 已验证的环境

| 项目 | 实际结果 |
|---|---|
| 训练系统 | WSL2，Ubuntu 24.04.3，内核 6.6.87.2-microsoft-standard-WSL2 |
| GPU | RTX 4070 SUPER，12282 MiB 显存，驱动 591.86 |
| 原运行时 GPU 占用 | 2075 MiB；剩余量随其他程序变化，不作为训练容量保证 |
| Windows 主机内存 | 系统报告 34,182,291,456 bytes，约 31.8 GiB |
| WSL 可见内存 | 约 15 GiB；检查时可用约 14 GiB，swap 4 GiB |
| 实际存储位置 | Ubuntu 虚拟磁盘位于 Windows C 盘；项目位于 WSL Linux 文件系统 |
| 可用空间 | WSL 文件系统报告 934 GiB；C 盘实际剩余约 294 GiB，预算按 C 盘余量控制 |
| Python / uv | 3.12.3 / 0.12.11 |
| PyTorch / NumPy | 2.11.0+cu128 / 2.5.3 |
| PyTorch CUDA 构建版本 | 12.8 |

版本以 `uv.lock` 和运行记录为准。这里的通过结论仅覆盖最小 CUDA 计算、反向传播、参数更新、内存快照恢复和环境重建，不代表大模型容量测试已完成。

## 依赖与重建

项目已经包含 `pyproject.toml`、`uv.lock` 和 `.python-version`。在有 Python 3.12.3、uv 和可用 NVIDIA 驱动的目标 WSL 主机上，从项目根目录执行：

```bash
uv sync --locked
uv run --locked python scripts/check_training.py
```

`uv sync --locked` 创建或同步项目 `.venv`；依赖配置与锁文件不匹配时会报错。软件包可以来自 uv 缓存或锁文件指定的下载源。完整重建证据使用原实验快照，在独立目录重新创建 `.venv`：

```bash
bash reports/m0-20260910-025345/rebuild.sh
```

该入口会新建 `/tmp/llm-m0-rebuild-XXXXXX`，并更新报告目录中的 `rebuild_*` 日志。已有通过记录无需反复执行；若要保留多次重建历史，应先复制报告目录。系统、驱动和 uv 的首次安装不在这次重建测试范围内。

平时通过 `uv add` 管理依赖，通过 `uv run --locked` 执行已固定的实验。修改依赖或训练语义后，使用新的 run ID 保存配置与结果。

## 存储和保留方式

以下是本项目默认约定，随阶段需要创建目录；当前未下载训练数据或模型权重。

| 内容 | 项目内相对路径 | 版本管理 |
|---|---|---|
| 原始与处理后的大数据 | `data/raw/`、`data/processed/` | 忽略 |
| 数据划分、来源、哈希清单 | `data/manifests/` | 跟踪 |
| 下载的基础模型 | `weights/` | 忽略 |
| 模型与数据缓存 | `.cache/` | 忽略；未来使用下载库时显式指向这里 |
| 训练检查点 | `checkpoints/<run_id>/` | 忽略 |
| 小规模配置、日志、评测与结论 | `reports/<run_id>/` | 跟踪 |
| 本地虚拟环境 | `.venv/` | 忽略 |

检查点默认保留最新两个可恢复状态，以及按开发指标选出的最佳状态（可以重合）；成功写入并验证新状态可加载后，才手动清理更早状态。配置、日志和失败实验结论长期保留，最终报告引用的检查点另行保留。没有启用任何自动删除任务。

空间按实际 C 盘余量规划，每次较大下载或训练前检查 `df -h /mnt/c .`。初期保守约定是给 C 盘保留至少 50 GiB 余量；这是项目操作预算，并非硬件要求。

## 尚未登记的个人安排

每周学习时长和 GPU 可用时段尚未提供，暂不安排固定周数或长时间训练。这些安排不影响 A0 中已验证的技术项；进入正式训练前再确定每轮预算。
