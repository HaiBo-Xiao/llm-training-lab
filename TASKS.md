# 当前任务与进度

更新时间：2026-09-10。

执行计划见 [PLAN.md](PLAN.md)，验收依据见 [ACCEPTANCE.md](ACCEPTANCE.md)。本文件只登记已发生的工作，不用计划中的预期结果代替实际结果。

## 已完成的准备

- [x] 学习者创建项目目录 `llm-training-lab`。
- [x] 保存修订后的计划、阶段验收标准与实验报告模板。

已完成目标 GPU 上的最小训练检查、实验记录、独立虚拟环境重建和主机资源核验，A0 已通过。证据见 [M0 报告](reports/m0-20260910-025345/REPORT.md)，环境和存储约定见 [ENVIRONMENT.md](ENVIRONMENT.md)。尚未开展 M1 模型训练。

## 当前唯一优先任务

M1-1 后半段：建立训练/验证划分，并把长文本切成定长训练样本。前半段（字符编码与 next-token 输入/目标位移）已完成。

## M1 进行中记录

- [x] M1-1 前半段：字符级编码与 next-token 输入/目标位移。证据见 [scripts/inspect_tokens.py](scripts/inspect_tokens.py)，提交 `dbc7cf1`；运行 `uv run --locked python scripts/inspect_tokens.py` 退出码为 0，脚本内 assert 全部通过。
- [ ] M1-1 后半段：训练/验证划分与定长样本切分；训练语料上练习一次 BPE 按 [PLAN.md](PLAN.md) 为可选。
- [ ] M1-2 及之后：小型 decoder-only 模型与训练循环。尚未开始，本阶段至今未运行任何模型训练。

边界：`scripts/inspect_tokens.py` 为纯 Python，未 import PyTorch，未构造张量、未计算 loss、未做反向传播，也未实现 causal mask。它只验证了编码往返与位移构造，可作为 A1「用固定序列检查目标位移」一项的部分证据，不覆盖 padding mask 与有效 token 数。

## M0 完成记录

- [x] M0-1：登记 GPU、Windows 主机内存、WSL 可见内存、实际空闲磁盘、系统与驱动。GPU 可用时段尚未提供，单列为后续预算信息。
- [x] M0-2：验证 CUDA 张量计算、梯度、参数更新和保存加载；保存加载已验证内存快照，未验证磁盘检查点。
- [x] M0-3：完成一次带 run ID、命令、配置、代码快照和输出的实验记录，见 `reports/m0-20260910-025345/`。
- [x] M0-4：保存重建入口与依赖锁文件；同一 WSL 主机的新虚拟环境重建通过，GPU 检查输出一致，退出码为 0。
- [x] M0-5：明确数据、权重与检查点位置和保留方式，补齐 `.gitignore`，见 `ENVIRONMENT.md`。
- [x] 按 A0 自查并填写证据，已具备进入 M1 的条件。

个人预算待补：每周学习时长、GPU 可用时段。暂不安排固定周数或长时间训练；这些信息不阻塞已验证的 A0 技术项。

当前无需执行完整 SFT/RL 安装、下载大型数据集或展开所有消融。

## 主线看板

| 阶段 | 内容 | 状态 | 验收 | 证据路径 / 结论 |
|---|---|---|---|---|
| M0 | 环境与记录 | 已通过 | A0 已通过 | [报告](reports/m0-20260910-025345/REPORT.md)；原运行及独立环境重建通过 |
| M1 | 从零训练基础 | 进行中 | A1 待验收 | M1-1 前半段完成，见 [scripts/inspect_tokens.py](scripts/inspect_tokens.py)（提交 `dbc7cf1`）；未运行模型训练 |
| M2 | 评测与短解答 SFT | 未开始 | A2 待验收 | — |
| M3 | Countdown 与 GRPO | 未开始 | A3 待验收 | — |
| M4 | GSM8K 三组闭环 | 未开始 | A4 待验收 | — |
| M5 | 四组对照与完整报告 | 未开始 | A5 待验收 | — |

## 扩展看板

| 扩展 | 内容 | 状态 | 建议时机 | 证据路径 |
|---|---|---|---|---|
| X1 | BabyLM 深入预训练 | 未开始（可选） | M1 后，按训练基础研究需求 | — |
| X2 | A/B/tie 偏好分类 | 未开始（可选） | M2 后 | — |
| X3 | 标量 Reward Model | 未开始（可选） | 理解成对偏好之后 | — |
| X4 | DPO | 未开始（推荐扩展） | M2 后，不阻塞 RLVR | — |
| X5 | OpenR1 / 更复杂数学 | 未开始（可选） | M4 后，容量与评测稳定时 | — |

## 如何更新

1. 开始实验时，将所在阶段标为“进行中”，用 [报告模板](reports/TEMPLATE.md) 记录研究问题与预算。
2. 实验结束后，保存配置、日志和逐样本结果，在报告中完成结论。
3. 对照验收标准自查，记录通过项、未完成项和证据路径。
4. 证据齐全时标为“已通过”；无收益结果按实现正确性与分析质量判定。
5. 因资源或依赖受阻时记录具体原因与已试方案；不把尚未完成的阶段勾成完成。
6. 同时更新下一项优先任务，保留已完成记录。

## 验收记录

- 日期：2026-09-10。
- 阶段 / 验收编号：M0 / A0。
- 结果：已通过；研究结果为不适用（本轮是最小环境与正确性检查）。
- 证据路径：`reports/m0-20260910-025345/`，重建与存储说明见 `ENVIRONMENT.md`。
- 已确认：目标 RTX 4070 SUPER 可进行计算、反向传播、更新和内存快照恢复；独立虚拟环境重建结果一致。
- 边界：未测试大型模型容量、磁盘检查点或独立进程恢复；学习/GPU 时间预算未登记。
- 下一步唯一优先任务：M1-1，文本编码与 next-token 输入/目标构造。

## 验收记录模板

```text
日期：
阶段 / 验收编号：
结果：已通过 / 未通过 / 部分完成
证据路径：
研究结果：提升 / 无显著变化 / 退化 / 不适用
已确认的原因与尚未验证的假设：
未完成项及原因：
下一步唯一优先任务：
```
