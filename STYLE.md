# 项目风格约定

这是一个指导型项目：助手编写练习骨架，学习者填写实现。本文件定义骨架、注释、自检、文档和提交的统一写法，优先于个人习惯和单个文件里的既有写法。

本文件是风格的唯一出处。所有协作者和 AI 助手（Claude Code、Codex 或其它）动手前先读这里；不为某个工具单独维护一份规则文件，也不在单个脚本里另立一套。风格要改先改本文件，再改代码。

## 0. 核心约定速览

1. 不替学习者写实现，练习槽位只留 `pass`。
2. 占位符只用 `pass`：函数体单独一行 `pass`；数据槽位写 `名称: 类型` 声明后跟一行 `pass`。不用 `raise NotImplementedError`、不用 `...`、不用假的返回值。
3. 题面按 LeetCode 规范写，小节名和顺序固定：描述、参数、返回、示例 N（输入 / 输出 / 解释）、约束、思路、提示。先说明用途，再用小数组完整对照原始输入、关键中间结果和最终产物；不能只报形状或只展示第一行。
4. TODO 从 1 起连续编号，按填写顺序排列，自检消息里带上编号。
5. 不改动学习者已经填写的答案区，先说明问题再等确认。
6. 自检 assert、打印区、边界声明由助手写好，并注明“已经写好，不需要修改”。
7. 环境校验类脚本由助手写成完整实现，训练与学习类练习由学习者执行。
8. 运行命令统一为项目根目录下的 `uv run --locked python scripts/<name>.py`。
9. 结论要有证据路径，不用计划中的预期结果代替实际结果，未验证的写“未验证”。

以下逐项展开。

## 1. 角色分工

- 助手写骨架、题面、自检和文档，不替学习者写实现。
- 学习者填写 TODO、运行脚本、记录证据。
- 助手不改动学习者已经填写的答案区。发现错误时说明问题和定位，等学习者确认后再动手；学习者明确要求 review 或修复时不受此限。
- 纯环境校验脚本（例如 `scripts/check_training.py`）由助手写成完整实现，不留 TODO；训练与学习类练习由学习者执行。

## 2. 练习脚本的结构

一个练习一个文件，放在 `scripts/`，文件名用 snake_case 动词短语，例如 `inspect_tokens.py`。运行命令统一为项目根目录下的 `uv run --locked python scripts/<name>.py`，并写进模块 docstring。文件自上而下线性阅读，顺序固定：

1. 模块 docstring：阶段编号与练习名、要完成的项数、填写顺序、运行命令、未完成时的预期行为、本轮的依赖范围；简要说明最终产物及数据从输入到产物的处理顺序。
2. 已给定的数据或常量，附注释说明本轮的约定。
3. 交替出现的「题面 + 槽位 + 自检」，按 TODO 顺序排列。
4. 打印区：展示结果，帮助学习者确认自己写对了。
5. 边界声明：这次验证了什么、没有验证什么。

模块 docstring 的样式：

```python
"""M1 第一个练习：字符编码，以及“预测下一个字符”的输入和目标。

你要完成的是三份数据、两个函数，以及两份训练序列。
请按 TODO 1～6 的顺序填写。打印和检查部分已经写好。

运行方式（在项目根目录）：
    uv run --locked python scripts/inspect_tokens.py

当前文件是练习骨架，未填写时会主动报错提示。这是预期行为。
本轮使用普通 Python，不需要 PyTorch，也不会执行模型训练。
"""
```

## 3. TODO 编号

- 全文件从 `TODO 1` 起连续编号，按填写顺序排列，不跳号、不复用、不加字母后缀。
- 每个槽位前必须有它的题面：数据槽位用 `#` 注释块，函数槽位用 docstring。
- 自检消息里引用编号，例如 `"请完成 TODO 4：encode"`，让报错直接对上题面。
- 一个 TODO 只要求一件事，需要两步的拆成两个 TODO。

## 4. 占位符：只用 pass

`pass` 是本项目唯一的「这里要你写代码」标记。不用 `raise NotImplementedError`，不用 `...`，不用假的返回值，不用注释掉的答案。

函数槽位：签名、docstring，然后单独一行 `pass`。

```python
def encode(s: str) -> list[int]:
    """TODO 4：把字符串转换成字符 ID 列表。

    ……（题面见第 5 节）
    """
    pass
```

数据槽位：先写 `名称: 类型` 声明，再跟一行 `pass`。

```python
# TODO 1：填写词表 vocab，这是数据，不是函数。
# 目标：包含 text 中所有不同的字符，每个字符只出现一次。
# 提示：set(text) 可以去重，sorted(...) 可以固定顺序。
vocab: list[str]
pass
```

`名称: 类型` 只是类型声明，不创建变量，所以未填写时运行会报 `NameError: name 'vocab' is not defined`，直接指出这个名字还没有被定义。函数槽位未填写时返回 `None`，由后面的 assert 给出中文提示。两种失败都是预期行为，模块 docstring 里要提前说明。

学习者用自己的实现替换 `pass` 行，类型声明行可以保留。骨架以外的地方不出现 `pass`：已经写好的示例代码不允许用 pass 占位。

## 5. 题面：按 LeetCode 规范写

题面的小节名和顺序固定，用不到的省略，不新增别名（不写“输入输出说明”“注意事项”这类自造标题）：

| 小节 | 内容 |
|---|---|
| 描述 | 开头的正文，不带标题；简要说明为什么需要这一步、要得到什么，以及产物供下一步做什么 |
| 参数 | 每个参数的含义，一行一个 |
| 返回 | 每个返回值的含义；数组还要说明形状，以及每一维和每个位置代表什么 |
| 示例 N | 具体的 `输入：` / `输出：` / `解释：`，至少一个；用完整的小数组对照输入与产物 |
| 约束 | 边界、前置假设、允许与禁止的做法 |
| 思路 | 可执行的步骤，不给代码 |
| 提示 | 语法或标准库线索，一条一行 |

类型信息由签名的类型提示承担，题面不再写一遍类型。数据槽位用 `目标：` 代替“描述”，其余小节相同。

函数槽位的完整写法：

```python
def decode(ids: list[int]) -> str:
    """TODO 5：把字符 ID 列表还原成字符串。

    按 itos 逐个查出字符，并保持原顺序拼接。

    参数：
        ids：字符 ID 列表，每个 ID 都在词表编号范围内。
    返回：
        还原出的字符串；是字符串，不是字符列表。
    示例 1：
        输入：ids = [2, 3, 1, 4, 0]
        输出："小猫吃鱼。"
        解释：词表为 ["。", "吃", "小", "猫", "鱼"]。
            原始 ID： [ 2,    3,    1,    4,    0 ]
            对应字符：["小", "猫", "吃", "鱼", "。"]
            按原顺序拼接字符，得到字符串“小猫吃鱼。”。
    约束：
        - 1 <= len(ids) <= 10^4
        - 0 <= ids[i] < len(vocab)，本轮不需要处理越界或未知 ID。
        - 不要把答案写死为全局变量 text。
    思路：
        1. 遍历 ids，把每个 ID 查成对应字符。
        2. 按原顺序拼接这些字符。
        3. return 拼接结果。
    提示：
        - 空字符串的 join 方法可以拼接字符列表。
    """
    pass
```

数组与切分示例的写法：

- 在题面的示例中直接列出具体输入，不能只写 `ids = train_ids` 让学习者回到上文推算。输入太长时先用独立的小数组演示，再说明它与实际语料的对应关系。
- 尽可能用数组展示数据变化；文本可以在字符串旁列出字符数组。涉及切分、位移、分组、堆叠、mask 等操作时，按“原始数组 → 选中的片段或分组 → 完整输出”对照展示。
- 每个示例都写全决定结果的配置与参数，例如 `block_size`、`batch_size`、样本编号和起点。为方便手算而缩小的配置必须标明是独立示例，不暗示需要修改脚本的实际配置。
- 数组示例要足够小，完整列出所有返回值；有 `x`、`y` 就展示两者，有 `xb`、`yb` 就展示两个完整的嵌套数组，每行单独排版。不能用省略号、形状说明或“第 0 行是……”代替其余结果。
- 解释每一行、每一列、编号和切分边界的含义；区分元素值与下标。若相邻片段有重叠或尾部被丢弃，指出具体元素及原因。
- 涉及起点或样本编号时，除第 0 项外，再给一个非零起点的例子。涉及未知元素、长度不足等边界时，说明本轮如何处理，或明确它不在合法输入范围内。
- 示例展示期望结果，思路说明实现步骤；完整的示例输出可以给出，通用实现仍由学习者填写。

例如，批处理题面应展示到下面这个程度（独立的小例子，配置与具体练习可以不同）：

```text
示例 1：
    输入：ids = [10, 11, 12, 13, 14, 15, 16]
          block_size = 3，batch_size = 2，k0 = 0
    输出：
        xb = [
            [10, 11, 12],
            [13, 14, 15],
        ]
        yb = [
            [11, 12, 13],
            [14, 15, 16],
        ]
    解释：原数组按下标编号为 0～6，数值 10～16 是示意 token ID。
          第 0 条取下标 0～3：[10, 11, 12, 13]，前 3 项是 x，后 3 项是 y。
          第 1 条取下标 3～6：[13, 14, 15, 16]，同样得到一对 x、y。
          两段在 ID 13 处重叠：它是上一条的最后一个目标，也是下一条的第一个输入。
          两条 x 按行放进 xb，两条 y 按相同顺序放进 yb。
          两个数组都是 2 行 3 列；行表示样本，列表示该样本内的位置。
          例如 xb[0][2] = 12 对应的预测目标是 yb[0][2] = 13。
```

其它规则：

- 示例值与本文件的数据一致。举与本例无关的例子时明确标注，例如“若词表为 `["a", "b"]`（与本例无关的例子）”。
- 数值范围用 LeetCode 记法：`1 <= len(s) <= 10^4`、`0 <= ids[i] < len(vocab)`。
- 约束要写明本轮不处理什么（未知字符、越界、空输入），避免学习者写多余的防御代码。
- 思路给步骤，不给答案表达式；一眼就能抄成代码的“提示”要改写。
- 题面不直接给出函数或槽位的通用实现，包括能直接填入答案区的切片表达式、推导式等；具体示例的完整输入输出不受此限。

## 6. 讲解注释

- 首次出现的概念、API、参数和缩写都解释一次；同一文件内不重复解释。
- 缩写给全称：`stoi` 是 string-to-integer 的缩写。
- 解释术语时说它做什么，不只给定义。
- 首次引入训练概念时，说明为什么需要它、它解决什么问题，并把解释连接到本题的具体产物。涉及数学含义时给出适合当前阶段的公式，逐一解释符号、适用条件，并用小数值例子核对。
- 例如，batch 要说明它把多条样本放在一起计算，能利用并行计算，并可将多条样本的损失取平均来计算梯度；区分批大小 B 与序列长度 T，说明它们分别对应数组的行与列。训练更新的说明须交代是否使用梯度累积，不能让学习者误以为组批本身就执行了训练。
- 关键参数逐个说明，例如 `set_to_none=True`、`weights_only=True`、`momentum=0.9`。
- 涉及数值的地方写出手算过程，让学习者能核对每一步结果。
- 函数内部的执行步骤用 ①②③ 标注；TODO 用阿拉伯数字，两套编号不混用。
- 中文全角标点，中英文之间空一格。标识符、参数名、文件路径保留原文，不翻译。
- 不写“显然”“很简单”“只需”这类判断，也不写与实现无关的鼓励语。
- 注释解释为什么这样做，或解释初学者会卡住的地方，不复述代码本身在做什么。

## 7. 自检与输出

- 每个槽位至少一条 `assert`。检查顺序与 TODO 顺序一致，先检查靠前的槽位，避免一次跳到后面尚未完成的部分。
- 消息用中文，说清该做什么并带上 TODO 编号，不泄露答案表达式。
- 检查区和打印区由助手写好，并在注释里说明“已经写好，不需要修改”。
- 浮点数用 `torch.allclose` 或 `torch.testing.assert_close` 比较，不用 `==`。
- 打印用 `print("标签：", 值)` 或 f-string，标签中文加全角冒号；需要控制格式时写明，例如 `f"{w.item():.6f}"`。
- 脚本最后打印一行「……检查通过」，作为整体通过标志。
- 不用 try/except 兜住练习中的错误，错误要暴露出来。
- 不引入测试框架，自检就是脚本里的 assert。

## 8. 边界声明

每个脚本末尾写清这次验证的边界：验证了什么、没有验证什么、结论不能推广到哪里。文档同样不用计划中的预期结果代替实际结果，未验证的写“未验证”。

## 9. 代码约定

- Python 3.12。类型提示用内置泛型 `list[int]`、`dict[str, int]`，不用 `typing.List`。
- 所有函数写类型提示。
- 只 import 本轮需要的依赖，纯 Python 练习不 import `torch`。
- 变量名用领域内通行的短名（`x`、`y`、`ids`、`logits`、`loss`），靠注释说明含义，不改成冗长命名。
- 不引入类和抽象层，除非练习目标就是它。
- 每个练习脚本独立运行，不 import 其它练习脚本。
- 依赖用 `uv add` 管理，实验用 `uv run --locked` 执行。

## 10. 文档与提交

- 文档用中文，一个一级标题，用表格登记状态、证据和路径，链接写相对路径。
- 新增文档同时登记到 [README.md](README.md) 的文档入口表。
- 结论必须给证据路径，阶段未通过就不勾成完成。
- 提交信息用中文，一行说清做了什么。
- 只在明确要求时新建 Markdown 文件。

## 11. 修改这套风格

风格变更先改本文件，再改代码，不在单个脚本里另立一套。本文件与已有脚本冲突时以本文件为准，并把脚本改过来。

## 附：最小骨架样板

一个数据槽位加一个函数槽位，包含本文件要求的全部要素。

```python
"""M1 练习样板：一份数据和一个函数。

你要完成的是一份数据和一个函数。请按 TODO 1～2 的顺序填写。
检查和打印部分已经写好。

运行方式（在项目根目录）：
    uv run --locked python scripts/example_exercise.py

当前文件是练习骨架，未填写时会主动报错提示。这是预期行为。
本轮使用普通 Python，不需要 PyTorch，也不会执行模型训练。
"""


# 原始文本。本轮规定一个字符就是一个 token，标点也算。
text = "小猫吃鱼。"


# TODO 1：填写词表 vocab，这是数据，不是函数。
# 目标：包含 text 中所有不同的字符，每个字符只出现一次，按字符升序排列。
# 示例 1：
#     输入：text = "aab"（与本例无关的小例子）。
#     输出：vocab = ["a", "b"]
#     解释：原始字符 ["a", "a", "b"] → 去重并排序 ["a", "b"]。
# 约束：排序只为让编号顺序稳定，不代表字符的重要性。
# 提示：set(...) 可以去重，sorted(...) 可以固定顺序。
vocab: list[str]
pass

def char_id(ch: str) -> int:
    """TODO 2：返回字符 ch 在词表中的编号。

    编号就是这个字符在 vocab 中的位置，从 0 开始。

    参数：
        ch：单个字符，且一定在 vocab 中。
    返回：
        它在 vocab 中的下标。
    示例 1：
        输入：ch = "小"
        输出：2
        解释：vocab 为 ["。", "吃", "小", "猫", "鱼"]，"小" 是第 3 项，下标为 2。
    约束：
        - 本轮不需要处理不在词表中的字符。
        - 直接使用上面建立的 vocab，不要在函数里重新计算词表。
    思路：
        1. 在 vocab 中查找 ch 出现的位置。
        2. return 这个位置。
    提示：
        - 列表有查找元素下标的方法。
    """
    pass


# 以下检查和打印已经写好，不需要修改。
# assert 后的条件不成立时，程序会在这里停止。
assert vocab == sorted(set(text)), "请完成 TODO 1：去重并排序的词表"
assert [char_id(ch) for ch in vocab] == list(range(len(vocab))), "请完成 TODO 2：按词表顺序返回下标"

print("词表：", vocab)
print("字符到编号：", {ch: char_id(ch) for ch in vocab})
print("样板练习检查通过")


# 本样板的边界：只演示写法，没有涉及张量、模型和训练。
```
