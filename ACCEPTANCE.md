# 验收标准与证据

对应 [PLAN.md](PLAN.md) 的 M0～M5 和 X1～X5；验收结论及证据路径登记在 [TASKS.md](TASKS.md)。M0 / A0 已通过，其余阶段尚未验收。

## 1. 如何判定完成

每阶段区分三个问题：

1. 实现是否正确：必要的数值、数据与恢复检查是否通过？
2. 实验是否可信：模型、数据、配置、评测与计算预算是否可追溯？
3. 研究目标是否达到：效果是否提升，证据是否支持最初假设？

前两项通过且第三项有充分记录和解释，可以完成学习阶段，即使结果无收益或退化。实现尚未验证的“负结果”不能作为完成证据。

状态使用“未开始、进行中、待验收、已通过、受阻、暂缓”。已通过必须附证据；资源不足或尚未执行不写成已通过。扩展暂缓不会阻塞主线。

## 2. 所有训练实验的共同要求

按当前阶段适用范围提供证据，不适用项写明原因。M0 环境检查无需模型或数据集 revision；M1 基础练习可以只有训练/验证划分。数学主项目从 M2 起必须明确训练、开发与最终测试的边界。

- [ ] 写明一个具体研究问题、预期现象、实验预算和停止条件。
- [ ] 保存 run ID、命令、完整配置、代码版本/快照、环境版本与随机种子。
- [ ] 保存模型/tokenizer revision、数据来源/版本、样本清单或可重建清单的规则。
- [ ] 保存训练、开发和测试的边界；同题不同轨迹不会跨区，已检查适用的跨来源重复。
- [ ] 给出训练与生成的实际 token 数、更新步数、耗时、峰值显存及吞吐口径。
- [ ] 报告训练曲线、适用的开发集指标和逐样本输出；未做最终测试时明确写“未评测”。
- [ ] 有失败/异常案例或已执行的边界检查，不为满足清单编造失败。
- [ ] 结论区分观察、解释与尚待验证的假设；下一步只优先选择一个问题。

单位测试与固定小样本用于损失、mask、verifier、恢复等容易产生错误的部分。无需为低影响文档或简单配置写形式化测试。

## 3. A0：环境与记录（对应 M0）

- [x] 确认检查运行在目标训练机，记录实际 GPU、主机内存、空闲磁盘、系统及软件版本。
- [x] CUDA 张量计算与反向传播成功；用非零梯度的受控例子验证参数确实更新，数值有限。
- [x] 最小对象/训练状态保存加载后可以继续运行。
- [x] 使用记录的安装步骤可以重建最小环境并重复检查；若尚未重建，保留该项未完成。
- [x] 一次 run 的代码、配置、命令、日志可以相互对应。

证据：环境记录、可重复检查入口及输出、最小运行记录、安装/版本说明。

2026-09-10 验收通过，见 [M0 报告](reports/m0-20260910-025345/REPORT.md)。保存加载验证范围为内存快照；环境重建范围为同一 WSL 主机上的新虚拟环境。

放行：进入小模型训练。不要求此时具备后续全部框架或通用实验平台。

## 4. A1：从零训练基础（对应 M1）

- [ ] 能画出或口述 token IDs → logits → next-token loss → backward → optimizer update 的流程与张量形状。
- [ ] 用固定序列检查目标位移、padding mask 和有效 token 数；不存在重复位移。
- [ ] 检查 causal mask：固定随机性后，改变后方 token 不应改变前方位置的预测。
- [ ] 模型在受控小批次上能过拟合；这用于检查训练机制，不用来证明泛化。
- [ ] 完成一次含验证集的稳定训练，记录 train/eval loss 和固定生成样本。
- [ ] 比较连续训练与保存恢复后的短期轨迹；记录允许的数值误差，说明数据顺序、随机数与优化器状态如何恢复。
- [ ] 能解释有效 batch、梯度累积的损失归一化、学习率、精度与主要显存来源。
- [ ] 至少一项消融有假设、预算、结果和解释；不要求取得最优配置。

证据：小批次检查、mask/位移检查、训练曲线、恢复对比和一次消融报告。

放行：进入 SFT。完整 BabyLM、架构对比和多词表研究不是额外门槛。

## 5. A2：评测与 SFT（对应 M2）

- [ ] 精确指定初始模型；Base 与 Instruct 命名无混用，使用匹配的 tokenizer 与模板。
- [ ] 首次训练前已保存题目级 train/dev/test 划分和去重检查；开发集可用于调参，测试集保持隔离。
- [ ] 固定题目集、prompt、chat template、解码、生成预算和答案判定，初始模型与 SFT 模型使用同一协议。
- [ ] 抽查至少 20 条样本，覆盖短/长样本、边界长度、EOS 和不同角色；不足 20 条时检查全部。
- [ ] 展示 token 与 label/mask 对照：应监督的位置有效，prompt/padding 按设计屏蔽，没有全 mask 样本。
- [ ] 记录长度分布、有效监督 token、截断率与筛选数量；正式训练样本保留完整目标答案。
- [ ] adapter 与匹配的基础模型在独立加载后可重现评测；记录量化与计算精度。
- [ ] 提供初始/SFT 逐题对照及错误分类，至少人工分析 20 道开发题。
- [ ] 完成 LoRA/QLoRA 的受控小实验或在更小规模下的机制对照；能解释低秩参数、冻结权重和计算 dtype。
- [ ] 分析效果、格式与长度变化。无提升时检查数据、实现和预算，并给出可验证解释。

证据：划分清单、评测配置、mask 可视化、训练记录、adapter 重载结果、逐题对比与方法对照。

放行：进入 RL 沙箱。不要求 SFT benchmark 必须提升，也不要求完成四组 SFT 超参数消融。

## 6. A3：Verifier 与 GRPO（对应 M3）

### 6.1 Verifier 正确性

- [ ] 任务规则写成确定的判定标准；生成的训练题符合规则且可解。
- [ ] 测试正确表达式、错误答案、非法字符/语法、重复数字、遗漏数字、额外数字、除零及资源上限。
- [ ] 模型输出作为数据解析，不允许任意代码执行；多答案、尾随文本等歧义有明确规则。
- [ ] 格式、合法性、正确性分别记录；人工核对至少 20 个模型输出与奖励。
- [ ] 训练与开发题分离；已测量初始成功率、全同奖励组占比和截断情况。

### 6.2 GRPO 数值正确性

- [ ] 手算一组 rewards、均值、标准差与 advantages；明确总体/样本标准差、epsilon 和归一化约定。
- [ ] 等奖励组在规定实现下得到零优势且不出现 NaN；G 满足所用算法要求。
- [ ] 区分当前策略、生成时旧策略与 KL 参考策略；old logprob 不参与梯度，参考策略按声明保持固定或更新。
- [ ] token logprob 对齐到实际生成 token；EOS、padding 和截断 mask 符合既定规则。
- [ ] 在相同权重、输入、数值路径与固定随机性下检查新旧 logprob/ratio；不把 ratio 的初值为 1 误判为没有梯度。
- [ ] 用正/负 advantage 与不同 ratio 的固定例子检查 clipping 分支及损失符号。
- [ ] 检查 KL 项的定义、方向与系数；相同分布时所用估计符合预期。
- [ ] 关闭 KL、熵奖励等额外目标且 advantage 为零时，策略目标的梯度为零；不要求带动量或 weight decay 的优化器更新后参数必然不变。
- [ ] 在非零信号的受控例子中验证梯度方向及更新，数值无 NaN/Inf。

### 6.3 框架对照

- [ ] 手写版与框架版固定 loss 类型、token/sequence 归一化、reward scaling、KL、更新次数和采样设置。
- [ ] 用固定轨迹优先比较中间量与目标；若定义不同，解释差异，不要求随机训练曲线完全一致。
- [ ] 核对生成 batch 与 G 的整除及 eval 约束，完成完整 rollout/backward/update 的容量实测。
- [ ] 记录 reward 各分量、组内方差、零优势组比例、长度、截断、熵、clip 指标、梯度与生成/更新耗时；未启用或未计算的 KL 指标如实标注。
- [ ] 能解释一次失败或边界现象，并完成一轮短训练及独立开发评测。

证据：verifier 测试、手算表、固定轨迹数值检查、手写/框架配置和短训练报告。

放行：进入数学 RLVR。成功率提升不是唯一门槛，但不能跳过实现正确性与有效学习信号的检查。

## 7. A4：数学 RLVR 闭环（对应 M4）

- [ ] E0、E1、E3 的权重关系清楚，模型与评测配置符合统一协议。
- [ ] SFT 与 RL 题目清单、重叠情况和开发/测试排除规则可查。
- [ ] verifier 覆盖整数、小数、分数、符号、千位分隔、适用的单位、空答案与多答案歧义；标准化不无条件删除影响语义的信息。
- [ ] RL 输入没有泄漏标准答案；奖励与独立评测均与人工检查一致。
- [ ] 完成可恢复的小规模 RL，保存配置、日志、逐题输出与成本。
- [ ] 正确率、解析成功率、格式、输出长度与截断分别报告；无效输出不能静默从分母中删掉。
- [ ] 人工比较至少 20 道开发题，分析格式收益、答案变化和奖励漏洞。
- [ ] 成功、无收益或退化均有证据，提出一个明确的后续检验；不声称三组结果已证明直接 RL 与 SFT 初始化的差异。

证据：三组开发评测、数学 verifier 检查、样本分析、恢复与容量记录。

放行：完成第一轮后训练闭环，进入完整研究对照。

## 8. A5：完整项目（对应 M5）

- [ ] E0/E1/E2/E3 四组全部执行，结果表注明相同初始 checkpoint、各组数据与训练预算。
- [ ] E3 从 E1 继续；E2 与 E3 的 RL 条件可比，差异已披露。
- [ ] 至少一项 SFT 与一项 RL 消融回答具体问题，报告训练/生成 token 和耗时，避免把更多计算误当成算法净收益。
- [ ] 最终测试协议事先确定，未利用测试题改 reward、选 checkpoint 或调超参数；若发生过，明确把结果降为开发性结果，并另设独立评估。
- [ ] 固定解码说明可重复性条件；随机评测给种子与采样次数，小幅提升结合逐题变化与不确定性分析。
- [ ] 报告成功与失败、数据处理、显存与吞吐、恢复、可迁移经验和未解决问题。
- [ ] 文档足以让另一人重建最小环境、准备数据、运行最小训练、加载权重和复现至少一项核心结果；至少按文档自行完成一次验证。
- [ ] 结论限定为实际完成的数据、模型与单卡条件，不把清单完成等同于所有算法工程岗位能力。

证据：最终报告、四组结果、消融报告、复现命令与复现检查记录。

## 9. 扩展验收

| 对应扩展 | 最低证据 |
|---|---|
| X1 BabyLM | 随机初始化证明、数据/训练轮数记录、tokenizer、官方评测协议与结果、至少一次有解释的消融；非标准设置清楚标注 |
| X2 偏好分类 | 划分与 log-loss；交换 A/B 后概率相应交换且 tie 概率基本稳定；位置/长度偏差和校准分析；无收益时给出有效诊断 |
| X3 标量 RM | 单回答标量接口、pairwise loss 检查、tie 处理、reward 差预测偏好、长度关联与 shortcut 案例 |
| X4 DPO | chosen/rejected 数据与来源、同 prompt 校验、策略/参考模型设置、损失检查、SFT/DPO 统一评测与成本；不把偏好 margin 当作真实质量的充分证明 |
| X5 复杂数学 | 独立训练/dev/test 来源与去重、完整轨迹筛选、答案等价判定边界检查、分领域及长度分析；MATH-500 不参与训练或调参 |

## 10. 日志口径

预训练/SFT 至少记录 loss、学习率、梯度范数、有效监督 token、吞吐、显存、步数与耗时。记录梯度范数时说明在裁剪前还是后。

RL 额外记录奖励分量及权重、组内奖励标准差/零方差组比例、completion 长度与截断、熵、clip 指标、适用的 KL、生成 token/s 与更新耗时。所有指标注明定义和聚合方式，框架默认不暴露的指标按任务需要补充。

Token 数区分输入、有效监督和生成；显存区分 allocated、reserved 与外部进程占用；评测区分 greedy accuracy、单次采样正确率与多次采样统计，不混用名称。
